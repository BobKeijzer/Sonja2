# FastAPI
fastapi>=0.115.0
uvicorn[standard]>=0.32.0

# CrewAI & tools (geen RAG-extra; custom RAG met Qdrant + Voyage)
crewai[anthropic]>=0.80.0
crewai-tools>=0.17.0
voyageai>=0.2.0
qdrant-client>=1.12.0

# Env
python-dotenv>=1.0.0

# CORS
python-multipart>=0.0.9

# News (RSS)
feedparser>=6.0.0

# Agenda recurring (cron)
croniter>=2.0.0

# E-mail via SMTP (stdlib smtplib; geen extra package)
